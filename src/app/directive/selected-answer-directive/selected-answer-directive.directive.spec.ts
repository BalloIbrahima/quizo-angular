import { SelectedAnswerDirectiveDirective } from './selected-answer-directive.directive';

describe('SelectedAnswerDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedAnswerDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
